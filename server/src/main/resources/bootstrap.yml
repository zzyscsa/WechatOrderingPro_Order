spring:
  application:
    name: order
  cloud:
    config:
      discovery:
        enabled: true
        service-id: CONFIG #注册中心config对应的应用名字
      profile: dev
eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/
